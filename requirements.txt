# SLIM web-service deps — this is what the Render web build installs
# (render.yaml buildCommand) and what the dashboard process actually imports.
#
# The pitch-pack PDF / logo-rasterising stack (weasyprint, cairosvg,
# pdfplumber, pypdf, pillow + the apt cairo/pango libs) is NOT here: the
# dashboard never imports it — only tool/pitch_pack.py does, which runs in the
# pitch-pack GitHub Action (that workflow installs the stack itself). Keeping
# it out of the web build keeps Render's slow free-tier builds fast/reliable.
# See requirements-pitchpack.txt for that stack; tests.yml installs both.
flask>=3.0
requests>=2.28
beautifulsoup4>=4.12
lxml>=4.9
python-dateutil>=2.8
gunicorn>=21.0
anthropic>=0.92    # semantic/universe model paths reachable from the dashboard
